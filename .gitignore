# ============================================================
#  Keil MDK (uVision 5) — STM32 项目 .gitignore
#  作用：忽略编译产物和临时文件，只让源码和工程文件进仓库
# ============================================================

# ---------- Keil 编译输出目录（都能重新生成） ----------
Objects/
Listings/
DebugConfig/
RTE/
.build/
build/
Output/
Debug/

# ---------- 编译中间产物 / 目标文件 ----------
*.o            # 目标文件
*.d            # 依赖文件
*.crf          # 交叉引用文件
*.lst          # 反汇编列表
*.map          # 内存映射文件
*.htm          # 编译报告
*.dep          # 依赖描述
*.lnp          # 链接器输入
*.axf          # 链接后的可执行文件
*.elf          # ELF 可执行文件
*.hex          # 生成的烧录文件
*.bin          # 生成的二进制文件
*.sct          # 链接器分散加载脚本（自动生成）
*.scvd         # 源码浏览数据
*.uvguix.*     # 界面窗口布局（每台机器不同，纯临时）

# ---------- 调试与日志 ----------
JLinkLog.txt
JLinkSettings.ini
*.log
*.trc
*.uvguix

# ---------- 备份与临时文件 ----------
*.bak
*.orig
*~
*.tmp
*.swp

# ---------- 操作系统 / 编辑器垃圾 ----------
Thumbs.db
desktop.ini
.DS_Store

# ============================================================
#  下面这些文件【必须保留】，这里列出来提醒大家，不要删：
#
#    *.uvprojx  —— Keil 工程主文件（工程的"户口本"）
#    *.uvoptx   —— 工程选项（含烧录/调试配置，同队共用一块板建议保留；
#                 如果以后发现它总引起冲突，再把它加进上面的忽略列表）
#    *.c / *.h  —— 源码
#    *.s        —— 启动汇编文件（startup_stm32f10x_*.s）
#    config.h   —— 我们统一维护的引脚 / 定时器配置
# ============================================================
